<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.modernodata</groupId>
        <artifactId>modern-odata-client</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>odata-client-test</artifactId>
    <name>OData Client Tests</name>
    <description>Integration tests for generated OData clients</description>

    <dependencies>
        <dependency>
            <groupId>com.modernodata</groupId>
            <artifactId>odata-client-core</artifactId>
        </dependency>
        <dependency>
            <groupId>com.modernodata</groupId>
            <artifactId>odata-client-runtime</artifactId>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <version>${slf4j.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Generate TripPin client sources -->
            <plugin>
                <groupId>com.modernodata</groupId>
                <artifactId>odata-client-maven-plugin</artifactId>
                <version>${project.version}</version>
                <executions>
                    <execution>
                        <id>generate-trippin</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <metadataFile>${project.parent.basedir}/odata-client-core/src/test/resources/trippin-metadata.xml</metadataFile>
                            <schemaPackages>
                                <Microsoft.OData.SampleService.Models.TripPin>com.example.trippin</Microsoft.OData.SampleService.Models.TripPin>
                            </schemaPackages>
                        </configuration>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>com.modernodata</groupId>
                        <artifactId>odata-client-core</artifactId>
                        <version>${project.version}</version>
                    </dependency>
                </dependencies>
            </plugin>

            <!-- Add generated sources to compile classpath -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.4.0</version>
                <executions>
                    <execution>
                        <id>add-source</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>add-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>${project.build.directory}/generated-sources/odata</source>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
